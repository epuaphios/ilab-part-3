create table orders (
                        order_id int,
                        customer_id int,
                        product_id int,
                        order_date date,
                        total_amount float
);